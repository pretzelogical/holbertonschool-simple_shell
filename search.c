#include "main.h"

/**
 * search_path- takes a parsed path and returns the first file in the
 * path that matches the command and executable.
 * @parsed_path: the parsed path to search
 *
 * Return: the full path of the file, if no file is found, return NULL.
*/
